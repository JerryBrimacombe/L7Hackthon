"""Drop a new module in this package and it is picked up automatically.

Contract: a factory returning any object with fit(X, y) and predict_proba(X).
Set `pretrained = True` on the object to skip fitting.
"""
